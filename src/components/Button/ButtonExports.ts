export enum ButtonType {
    /** Icon only */
    Short = "short",
    /** Will fit available length but is only suited for when there is space for an icon and a text */
    Long = "long",
}
export enum ButtonStyle {
    Primary = "primary",
    Secondary = "secondary",
    Accent = "accent",
    Settings = "settings",
    Delete = "delete",
    Disabled = "disabled",
    DisabledDark = "disabledDark",
    Correct = "correct",
}